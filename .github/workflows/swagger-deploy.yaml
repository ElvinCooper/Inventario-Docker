name: Deploy Swagger to SwaggerHub

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy-swagger:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_pass
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Start Flask app
        env:
          SQLALCHEMY_DATABASE_URI: postgresql://test_user:test_pass@localhost:5432/test_db
          JWT_SECRET_KEY: test-secret-key
          SECRET_KEY: test-secret
          FLASK_ENV: development
        run: |
          python -m flask --app app.run run --host=0.0.0.0 --port=8000 &
          sleep 5

      - name: Generate OpenAPI spec
        run: |
          curl http://localhost:8000/openapi.json -o openapi.json

#      - name: Deploy to SwaggerHub
#        uses: smartbear/swaggerhub-cli@v0.9.0
#        env:
#          SWAGGERHUB_API_KEY: ${{ secrets.SWAGGERHUB_API_KEY }}
#        with:
#          args: api:update INGELVIN01COOPER/inventario-api/v1.0.0 -f openapi.json --visibility public --published=publish --force